Requirement:
=> app người dùng sài.
+ nghĩ tới cách cài đặt.
+ tinh năng: lúc cài đặt lấy source code mới nhất rồi bỏ VÀO THƯ MỤC VÀ SÀI. 
Không có quyền đăng nhập hay sửa.
+ version 1.0 => 
download file zip rồi bỏ vào thư mục cài đặt thành file zip. 
sau khi người dùng cài đặt giải nén sẽ có sẵn các repor. 
porttable => k cần cài đặt => zip thành file => đưa file zip có sẵn exe => vue, node,......
+ khi mà repo update:
tạo 1 version mới hoặc làm bằng tay => đóng gói lại (developer) 
vd: template folder => bỏ file zip vô 
=> đóng gói nguyên 1 thư mục thành file exe 
=> 1 version mới 
=> đưa user sử dụng.

=> requiment:
+ không cần user đăng nhập, ký
+ 2 phần:
tải về, update repo

Đã hoàn thành:
+ tải source code xuống từ girtlab nhưng vẫn phải đăng nhập.
+ hoàn thành UI.

Chưa hoàn thành:
+ chưa thể thực hiện theo yêu cầu ở phía trên như: tạo file .exe, làm porttable.... 